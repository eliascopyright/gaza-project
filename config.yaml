paths:
  base: .
  raw: data
  extracted: data_extracted/zip
  bronze: data/bronze
  silver: data/silver
  gold: data/gold
  maps: maps
  logs: logs

query:
  gaza: gaza boundaries
  thailand: thailand subnational boundaries
  
filters:
  formats: [SHP, GEOJSON, CSV]
